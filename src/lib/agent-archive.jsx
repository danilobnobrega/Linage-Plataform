// Archived: original Agent page design (hero banner + stats panel layout).
// This design was shared by deactivated agents (ashe, jace, aiden, venn, dexter).
// Not rendered anywhere in the platform.

/*
import React, { useState, useEffect, useRef } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import { useAuth } from '@clerk/clerk-react';
import useStore from '../store';
import {
  Send, Sparkles, Check, FileText, FileCheck, RefreshCw,
} from 'lucide-react';
import { useDecryptPlaceholder } from '../hooks/useDecryptPlaceholder';
import { fetchNewsForTopic } from '../lib/news';

const CHAT_PHRASES = [
  'Qual tema quer desenvolver hoje?',
  'Compartilhe sua ideia bruta...',
  'O que você quer transformar em post?',
  'Descreva seu ponto de vista...',
  'Qual conceito quer posicionar?',
];

function Agent() {
  const { id } = useParams();
  // ... hero banner layout with agent-hero-banner, agent-grid-layout, agent-detail-panel
  // Full source preserved in git history (commit before this archive was created).
}

export default Agent;
*/
